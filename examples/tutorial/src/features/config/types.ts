/**
 * Game configuration settings
 */
export interface GameConfig {
  /** BGM volume (0.0 - 1.0) */
  bgmVolume: number;
  /** SE volume (0.0 - 1.0) */
  seVolume: number;
  /** Text display speed (0.0 - 1.0, higher = faster) */
  textSpeed: number;
}
