pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,js,tsx,jsx,css,html}"
      root: "."
      stage_fixed: true
      run: bun run lint
    format:
      glob: "*.{ts,js,tsx,jsx,css,html}"
      root: "."
      stage_fixed: true
      run: bun run format

pre-push:
  parallel: true
  commands:
    test:
      glob: "*.{ts,js,tsx,jsx,css,html}"
      root: "."
      run: bun test
    build:
      glob: "*.{ts,js,tsx,jsx,css,html}"
      root: "."
      run: bun run build
